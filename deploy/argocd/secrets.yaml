apiVersion: v1
kind: Secret
metadata:
  name: pidor-bot-2-secrets
  namespace: pidor
stringData:
  pg_url: "123"
  tg_token: "322"

# Note: CHAT_WHITELIST is no longer used. Game configuration is now managed via ConfigMap.
# See deploy/charts/app/templates/configmap.yaml and deploy/charts/app/values.yaml
